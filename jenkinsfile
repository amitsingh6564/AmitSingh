pipeline {
    agent {
          node {
             label 'SuperAgent'
           }
        }

    stages {
        stage('Build') {
            container ('node') {
            steps {
                echo 'Building..'
                sh '''
                '''
              }
            }
        }
    }

    post {
        success {
            echo 'This will run only if successful'
        }
    }
}
