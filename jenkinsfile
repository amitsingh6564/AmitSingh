pipeline {
    agent {
          node {
             label 'SuperAgent'
           }
        }

    stages {
        stage('Build') {
          agent any
           
            steps {
                echo 'Building..'
                sh '''
                '''
            }
        }
    }

    post {
        success {
            echo 'This will run only if successful'
        }
    }
}
