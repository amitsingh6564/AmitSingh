pipeline {
    agent {
          node {
             label 'SuperAgent'
           }
        }

    stages {
        stage('Build') {
           
            steps {
                echo 'Building..'
                sh '''
                '''
            }
        }
    }

    post {
        success {
            echo 'This will run only if successful'
        }
    }
}
